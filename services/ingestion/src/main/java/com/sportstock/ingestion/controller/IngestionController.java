package com.sportstock.ingestion.controller;

/**
 * Placeholder kept intentionally to avoid hard deletes in restricted environments.
 * Endpoint mappings are split into dedicated controller files.
 */
public class IngestionController {
}
